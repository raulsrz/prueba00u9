{% extends 'base.html.twig' %}

{% block body %}
    <h1>Codigo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ codigo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ codigo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('codigo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('codigo_edit', { 'id': codigo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
